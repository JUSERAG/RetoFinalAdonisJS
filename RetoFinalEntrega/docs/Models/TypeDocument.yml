definitions:
  TypeDocument:
    type: object
    properties:
      get:
        type: object
        properties:
          id:
            type: number
          name:
            type: string
components:
  schemas:
    createTypeDocument:
        type: object
        properties:            
            name:
              type: string
              description: nombre del tipo de documento a registrar
        required:
          - name
        example:
          {
            "name": "C.C. Extranjera"
          }
    updateTypeDocument:
      type: object
      properties:
        name:
          type: string
          description: nombre del tipo de documento
      required:
      example:
        {
          "name": "T.I."
        }
    
